#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <cmath>
using namespace std;

int main()
{
	int n;
	cin >> n;
	// 제곱수
	int ans = pow(n, 0.5);
	cout << ans;
	return 0;
}